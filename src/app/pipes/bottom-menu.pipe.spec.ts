import { BottomMenuPipe } from './bottom-menu.pipe';

describe('BottomMenuPipe', () => {
  it('create an instance', () => {
    const pipe = new BottomMenuPipe();
    expect(pipe).toBeTruthy();
  });
});
